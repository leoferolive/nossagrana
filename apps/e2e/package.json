{
  "name": "@nossagrana/e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "setup:db": "docker-compose -f docker-compose.e2e.yml up -d"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "wait-on": "^8.0.3"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
